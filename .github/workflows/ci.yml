name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx astro check

      # Node адаптер (без WORKERS_CI) — Cloudflare build-ът се валидира от
      # Workers Builds при деплой; тук пазим бърз, офлайн-съвместим сигнал.
      - name: Build
        run: npm run build
